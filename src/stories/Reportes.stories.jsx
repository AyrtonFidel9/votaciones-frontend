import React from 'react';
import { Reportes } from '../pages';

export default {
    title: 'Paginas/Reportes',
    component: Reportes,
}

const Template = (args) => <Reportes {...args}/>

export const PaginaReportes = Template.bind({});