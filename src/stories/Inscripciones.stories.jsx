import React from 'react';
import { Inscripciones } from '../pages';

export default {
    title: 'Paginas/Inscripciones',
    component: Inscripciones,
}

const Template = (args) => <Inscripciones {...args}/>

export const PaginaInscripciones = Template.bind({});