import { Button } from "@mui/material";
import styled from "styled-components";

const AnularBtn = styled(Button)`
    width: 93%;
    margin: 0 auto;
    display: none !important;
`;

export default AnularBtn;
